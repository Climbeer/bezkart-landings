# Lessons Learned

## 2026-04-01
- Bootstrapped the repository structure from `AGENTS.md` guidance.
- Added a deterministic preflight script so environment failures are caught early.
- Added a capability registry immediately to avoid ad-hoc tool discovery later.
